name: Miami commercial path v3

on:
  push:
    paths:
      - '.github/workflows/oneoff-miami-commercial-v3-20260912.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply Miami commercial patch
        run: python3 .github/scripts/fix_miami_commercial_20260912_v3.py
      - name: Commit changes
        run: |
          git config user.name 'DIGIY LYFE'
          git config user.email 'baptistejb24@gmail.com'
          git add index.html miami.html
          git commit -m 'fix: guide Miami visitors through full offer'
          git push
